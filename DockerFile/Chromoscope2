FROM node:18-alpine

RUN apk add --no-cache git

RUN git clone https://github.com/eugeniomazzone/chromoscope.git
WORKDIR /chromoscope
RUN git checkout human

RUN yarn install
CMD ["yarn","start","--host","--port","8080","--https","--cors","--base", "/chromoscope/"]
